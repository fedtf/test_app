.well {{user.firstName}}
.well {{user.lastName}}
ul.list-group
    li.list-group-item.clearfix(ng-repeat="table in userTables track by $index")
        a(ng-href="#tables/{{table._id}}") {{table.tableName}}

        span.pull-right
            button.btn.btn-info.marged-right Редактировать
            button.btn.btn-danger.marged-right(ng-click="deleteTable(table, $index)") Удалить
            button.btn.btn-default(type="button", data-toggle="dropdown", aria-expanded="false") Сохранить как...
                span.caret
            ul.dropdown-menu(role="menu")
                li
                    a(ng-click="exportTable(table, 'xlsx')") Файл MS Excel
                li
                    a(ng-click="exportTable(table, 'docx')") Файл MS Word
