.page-header
    h1.main-header Добро пожаловать <br> Введите данные для создания матрицы тестирования
.row
    form(name="mainForm")
    .col-sm-6.text-center
        .col-xs-10.col-xs-offset-1
            label.main-label(for="studentsNumber") Количество учеников:
            .col-xs-offset-1.col-xs-10
                i.icon-up-open(ng-click="numbers.studentsNumber = inc(numbers.studentsNumber)")
                input.form-control.main-input(name="studentsNumber", ng-model="numbers.studentsNumber",
                ng-click="numbers.studentsNumber = inputClick(numbers.studentsNumber)", required)
                i.icon-down-open(ng-click="numbers.studentsNumber = dec(numbers.studentsNumber)")
    .col-sm-6.text-center
        .col-xs-10.col-xs-offset-1
            label.main-label(for="problemsNumber") Количество заданий:
            .col-xs-offset-1.col-xs-10
                i.icon-up-open(ng-click="numbers.problemsNumber = inc(numbers.problemsNumber)")
                input.form-control.main-input(name="problemsNumber", ng-model="numbers.problemsNumber",
                ng-click="numbers.problemsNumber = inputClick(numbers.problemsNumber)", required)
                i.icon-down-open(ng-click="numbers.problemsNumber = dec(numbers.problemsNumber)")
.row.marged-top
    .col-xs-10.col-xs-offset-1
        .btn-group.col-xs-12
            a.btn.btn-default.col-xs-6(type="button", ng-href="#create-table/{{numbers.studentsNumber}}/{{numbers.problemsNumber}}", ng-disabled="!(numbers.studentsNumber > 0) || !(numbers.problemsNumber > 0)") Cоздать таблицу
            button.btn.btn-default.col-xs-6(ng-file-change="upload($files)", ng-file-select="") Из файла